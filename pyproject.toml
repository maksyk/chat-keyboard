[tool.poetry]
name = "chat-keyboard"
version = "0.1.0"
description = ""
authors = ["maksy <maksykaryak@mail.ru>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"
django = "^5.0.1"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
